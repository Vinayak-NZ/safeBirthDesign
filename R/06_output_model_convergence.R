## ---- bayesian-model-convergence-stats

bayesian_model_comm_convergence <- bayesian_convergence(
  model_input = bayesian_model_comm
)

bayesian_model_safe_convergence <- bayesian_convergence(
  model_input = bayesian_model_safe
)

bayesian_model_hapa2_convergence <- bayesian_convergence(
  model_input = bayesian_model_hapa2
)

bayesian_model_hapa3_convergence <- bayesian_convergence(
  model_input = bayesian_model_hapa3
)

bayesian_model_hapa5_convergence <- bayesian_convergence(
  model_input = bayesian_model_hapa5
)