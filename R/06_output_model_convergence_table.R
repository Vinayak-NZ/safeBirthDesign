## ---- create-convergence-table

comm_convergence_table <- convergence_table("comm")

comm_convergence_table <- convergence_table("safe")

comm_convergence_table <- convergence_table("hapa2")

comm_convergence_table <- convergence_table("hapa3")

comm_convergence_table <- convergence_table("hapa5")
